apiVersion: v1
kind: Service
metadata:
  name: wibe-studio
spec:
  selector:
    app: wibe-studio
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
  externalIPs:
    - 10.0.2.2   # vm1 IP — keeps Mikrotik DNAT working exactly as before